
import { Toaster } from "@/components/ui/toaster";
import { Toaster as Sonner } from "@/components/ui/sonner";
import { TooltipProvider } from "@/components/ui/tooltip";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { BrowserRouter, Routes, Route } from "react-router-dom";

import Index from "./pages/Index";
import Chemistry from "./pages/Chemistry";
import Contact from "./pages/Contact";
import NotFound from "./pages/NotFound";

const queryClient = new QueryClient();

const App = () => (
  <QueryClientProvider client={queryClient}>
    <TooltipProvider>
      <Toaster />
      <Sonner />
      <BrowserRouter>
        <Routes>
          <Route path="/" element={<Index />} />
          <Route path="/chemistry" element={<Chemistry />} />
          <Route path="/contact" element={<Contact />} />
          {/* These routes would be implemented later */}
          <Route path="/computer-science" element={<Chemistry />} />
          <Route path="/mathematics" element={<Chemistry />} />
          <Route path="/physics" element={<Chemistry />} />
          <Route path="/english" element={<Chemistry />} />
          <Route path="/nepali" element={<Chemistry />} />
          {/* ADD ALL CUSTOM ROUTES ABOVE THE CATCH-ALL "*" ROUTE */}
          <Route path="*" element={<NotFound />} />
        </Routes>
      </BrowserRouter>
    </TooltipProvider>
  </QueryClientProvider>
);

export default App;
